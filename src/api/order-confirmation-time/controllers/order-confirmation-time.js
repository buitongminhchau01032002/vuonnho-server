'use strict';

/**
 * order-confirmation-time controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::order-confirmation-time.order-confirmation-time');
